spring.datasource.url=jdbc:mysql://localhost:3307/eleg?serverTimezone=Europe/Berlin&useLegacyDatetimeCode=false
spring.datasource.username=SIFE
spring.datasource.password=sife


# DataSource: schema, data initialization
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=never
# spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JPA Section
spring.jpa.show-sql=false

# Set Logging
#logging.file.name = target/sefreports.log

logging.level.org.springframework.web=DEBUG

# Set Server
# server.port=8080

# Mail Server Properties (EP)
# spring.mail.host=APPLANBRU.EP.PARL.UNION.EU
# spring.mail.port=25


# Other smtp properties
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.properties.mail.smtp.writetimeout=5000

# environment=prod